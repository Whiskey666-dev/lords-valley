/**
 * FogOverlay — Componente React para niebla de guerra.
 * Delega la renderización a Phaser FogOfWarSystem para 60 FPS sin sobrecarga del DOM.
 */
export function FogOverlay() {
  return null;
}

export default FogOverlay;
